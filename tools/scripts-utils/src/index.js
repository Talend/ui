const fs = require('./fs');
const babel = require('./babel');
const pkg = require('./pkg');
const path = require('./path');
const process = require('./spawn');

module.exports = {
	fs,
	babel,
	pkg,
	path,
	process,
};
