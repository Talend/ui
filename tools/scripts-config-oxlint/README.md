# @talend/scripts-config-oxlint

Shared [oxlint](https://oxc.rs/docs/guide/usage/linter.html) configuration used across Talend/UI packages.

## Usage

Add an `oxlint.config.mts` file at the root of your package:

```ts
import config from '@talend/scripts-config-oxlint';

export default config;
```
