name: PR lint

on:
  pull_request:

permissions:
  contents: write
  deployments: read
  pull-requests: write # needed to annotate code
  statuses: write
  checks: write

# This allows a subsequently queued workflow run to interrupt previous runs
concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    name: Lint

    steps:
      - name: Checkout sources
        uses: actions/checkout@8f4b7f84864484a7bf31766abe9204da3cbe65b3 #v3.5.0
        with:
          persist-credentials: false # otherwise, the token used is the GITHUB_TOKEN, instead of your personal token
          fetch-depth: 0 # otherwise, you will failed to push refs to dest repo

      - name: Use Node.js
        uses: actions/setup-node@64ed1c7eab4cce3362f8c340dee64e5eaeef8f7c # v3.6.0
        with:
          node-version: 16
          check-latest: true
          registry-url: "https://registry.npmjs.org/"
          scope: "@talend"
          cache: "yarn"

      - name: Install
        run: yarn install --frozen-lockfile --ignore-scripts

      - name: eslint + stylelint
        continue-on-error: true
        id: eslint
        run: yarn lint

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@c41b7152594c4423f3787d26662239eb0ae027c0 #v29.0.4

      - name: Merge lint
        run: node merge-report.js ${{ steps.changed-files.outputs.all_changed_files }}

      - name: Annotate Code Linting Results
        uses: ataylorme/eslint-annotate-action@31e4193e25e99054c9309570ff45a83d9e1fcaf0 #v2.2.0
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          report-json: ./eslint-report.json
