language: node_js
cache:
  directories:
    - $HOME/.cache/yarn
notifications:
  email: false
env:
  - SLIMERJSLAUNCHER=$(which firefox) DISPLAY=:99.0 PATH=$TRAVIS_BUILD_DIR/node_modules/slimerjs/src:$PATH
addons:
  firefox: "50.0"
node_js:
  - '6'
before_install:
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/before_install.sh
before_script:
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && sh -e /etc/init.d/xvfb start
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && yarn global add sassdoc http-server surge
sudo: required
script:
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && yarn test
after_success:
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/after_success_lint.sh
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/after_success_static.sh
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/after_success_screenshots.sh
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/after_success_git.sh
  - [ $TRAVIS_COMMIT_MESSAGE =~ "test(ci)*" ] && .travis/deploy.sh
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
