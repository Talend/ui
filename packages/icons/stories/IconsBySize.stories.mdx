import { Meta, IconGallery, IconItem } from '@storybook/addon-docs';

import Icon, { IconColorTokenSelect } from './Icon';
import { icons } from '../src/icon';

<Meta title="Icons by size" />

# Icons

<IconColorTokenSelect />
{Object.keys(icons).map((size, i) => (
	<section key={i}>
		<h2>{size}</h2>
		<IconGallery>
			{Object.values(icons[size]).map((name, j) => (
				<IconItem key={j} name={name}>
					<Icon size={size} name={name} />
				</IconItem>
			))}
		</IconGallery>
	</section>
))}
