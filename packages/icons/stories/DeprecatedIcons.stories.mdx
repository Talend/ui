import { Meta, IconGallery, IconItem } from '@storybook/addon-docs';

import Icon from './Icon';
import { legacyIcons } from '../src/icon';

<Meta title="Deprecated/Icons" />

# Deprecated icons

Deprecated icons will be removed in the next major version.

<IconGallery>
	{Object.keys(legacyIcons)
		.reduce((acc, key) => {
			acc = acc.concat(legacyIcons[key]);
			return acc;
		}, [])
		.map((name, index) => (
			<IconItem key={index} name={name}>
				<Icon name={name} />
			</IconItem>
		))}
</IconGallery>
