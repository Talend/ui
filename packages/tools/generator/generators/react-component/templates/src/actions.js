// function fnName(event, data, context) {
//     return {
//         type:
//     };
// }

export default {
    // key is the registry key, by convention use the componentName in it
    // '<%= props.name %>#fnName': fnName
};
