import React from 'react';
import { shallow } from 'enzyme';

describe('CMFRoute', () => {
	it('should instantiate Route component', () => {
		// given

		// when

		// then
	});

	it('should instanciate its nested Route component', () => {
		// given

		// when

		// then
	});
});
