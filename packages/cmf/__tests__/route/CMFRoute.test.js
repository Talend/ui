import React from 'react';
import { shallow } from 'enzyme';

describe('CMFRouteHook', () => {
	it('should render its children', () => {
		// given

		// when

		// then
	});

	it('should create onEnter function', () => {
		// given

		// when

		// then
	});

	it('should create onLeave function', () => {
		// given

		// when

		// then
	});

	it('should call onEnter when componentWillMount', () => {
		// given

		// when

		// then
	});

	it('should call onLeave when componentWillUnmount', () => {
		// given

		// when

		// then
	});
});
