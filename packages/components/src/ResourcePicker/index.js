import ResourcePicker from './ResourcePicker.component';
import { SORT_OPTIONS, STATE_FILTERS } from './Toolbar';

const TOOLBAR_OPTIONS = {
	SORT_OPTIONS, STATE_FILTERS,
};

export default ResourcePicker;
export { TOOLBAR_OPTIONS };
