import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { FigmaImage, Use } from '~docs';

import Switch from '../';

<Meta
	title="Components/Toggle button"
	component={Switch}
	parameters={{
		status: { figma: 'ok', storybook: 'ok', react: 'ok', i18n: 'na' },
		chromatic: { disableSnapshot: true },
	}}
/>

# Toggle button

Toggle button is a quick way to control a change in context. Toggle buttons can have more than two options compare to a Toggle switch, but there is always a default selection.

## Zoning

<FigmaImage
	src="https://www.figma.com/file/K1qtSHm69U8tkgKTHqlrXU/mhuet%2Fswitch?node-id=719%3A34994"
	alt="zoning image for toggle button"
/>

## Style

<Canvas>
	<Story name="default">
		<Switch values={['input', 'both', 'output']} />
	</Story>
</Canvas>

## States

<FigmaImage
	src="https://www.figma.com/file/K1qtSHm69U8tkgKTHqlrXU/mhuet%2Fswitch?node-id=706%3A35029"
	alt="states image for toggle button"
/>

## Content

Labels for switches should give users a strong indication of what they are selecting. Limit the label placed inside the switch to one word, a noun or an adjective, that describes the functionality of the control.

<Use>
	<Use.Do>
		<ul>
			<li>Use sentence case in switch labels.</li>
			<li>Keep labels for switches short and direct.</li>
			<li>
				For on/off switches, use the label to describe what the control will do when the switch is
				on.
			</li>
		</ul>
	</Use.Do>
	<Use.Dont>
		<ul>
			<li>Don't use neutral or ambiguous labels.</li>
			<li>Don't use title case or all caps in switch labels (except for acronyms).</li>
		</ul>
	</Use.Dont>
</Use>

## Interaction

## Accessibility

## Usage

<Canvas>
	<Story name="uncontrolled">
		{() => {
			const defaultValue = 'value f';
			const [value, setValue] = React.useState(defaultValue);
			return (
				<>
					<p>Selected value: {value}</p>
					<Switch
						values={['value a', 'value b', 'value c', 'value d', 'value e', 'value f']}
						defaultValue={defaultValue}
						onChange={(e, v) => setValue(v)}
					/>
				</>
			);
		}}
	</Story>
</Canvas>
