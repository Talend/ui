import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import Skeleton, { SkeletonButton, SkeletonParagraph, SkeletonHeading } from '.';
import { StackHorizontal, StackVertical } from '../Stack';

import * as Stories from './Skeleton.stories';

<Meta
	title="Components/Skeleton"
	parameters={{
		status: { figma: 'wip', storybook: 'wip', react: 'ok', i18n: 'na' },
	}}
/>

# Skeleton

Skeleton is used as decoration to fill the space while the real content is loading.

## Zoning

## Style

### Default

// Todo

### Button

<Canvas>
	<Story story={Stories.SkeletonButtonStory} name="Button" />
</Canvas>

<ArgsTable story="Button" />

### ButtonIcon

<Canvas>
	<Story story={Stories.SkeletonButtonIconStory} name="ButtonIcon" />
</Canvas>

<ArgsTable story="ButtonIcon" />

### Text

#### Heading

It illustrates any heading element.

<Canvas>
	<Story story={Stories.SkeletonHeadingStory} name="Headings" />
</Canvas>

<ArgsTable story="Headings" />

#### Paragraph

It illustrates any paragraph or small copy element.

<Canvas>
	<Story story={Stories.SkeletonParagraphStory} name="Paragraphs" />
</Canvas>

<ArgsTable story="Paragraphs" />

#### Composition example

Combine all of them to mock content!

<Canvas>
	<Story name="Composition">
		<StackVertical gap="XS" align="stretch">
			<SkeletonHeading />
			<StackVertical gap="XXS">
				<SkeletonParagraph />
				<SkeletonParagraph />
				<SkeletonParagraph />
			</StackVertical>
			<StackHorizontal gap="XS" justify="end">
				<div style={{ width: '50%' }} />
				<StackHorizontal gap="XS" align="center">
					<SkeletonParagraph size="S" />
					<SkeletonButton />
				</StackHorizontal>
			</StackHorizontal>
		</StackVertical>
	</Story>
</Canvas>

## States

Not Applicable

## Content

Not Applicable

## Interaction

Skeletons use the heartbeat animation.
The cursor is set to `progress`.

## Accessibility

They are unitary hidden for screen readers by default.
The goal is to set the current loading content to `aria-busy="true"` instead.
Here is an article about [more accessible skeletons](https://adrianroselli.com/2020/11/more-accessible-skeletons.html) written by Adrian Roselli.

## Usage

As you've seen above, `Skeleton` can be used by importing the single variant you need (ie: `SkeletonButton` or `SkeletonHeading`).

You can also use the `Skeleton` and specify the variant you mean to use: its props will then match the variant's own.

<Canvas>
	<Story story={Stories.ButtonVariants} />
</Canvas>
