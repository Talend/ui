import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { WithSelector, FigmaImage } from '../../../docs';
import Form from '../';

<Meta title="Components/Form/Fields/Number" />

# Number

It's used to let the user enter a number.
They include built-in validation to reject non-numerical entries.
The browser may opt to provide stepper arrows to let the user increase and decrease the value using their mouse or by tapping with a fingertip.

## Zoning

<FigmaImage
	src="https://www.figma.com/file/qc8oUSyVJM67gSsbTShftt/%F0%9F%93%90-Zoning?node-id=548%3A53"
	alt="zoning image for number inputs"
/>

## Style

## States

### Default

<Canvas>
	<Story name="default">
		<Form.Number label="Number" min="10" max="100" />
	</Story>
</Canvas>

### Hover

<Canvas withSource="none">
	<Story
		name="hover"
		decorators={[storyFn => <WithSelector selector=":hover">{storyFn()}</WithSelector>]}
	>
		<Form.Number label="Number" />
	</Story>
</Canvas>

### Focus

<Canvas withSource="none">
	<Story
		name="focus"
		decorators={[storyFn => <WithSelector selector=":focus">{storyFn()}</WithSelector>]}
	>
		<Form.Number label="Number" />
	</Story>
</Canvas>

### Filled

<Canvas>
	<Story name="filled">
		<Form.Number label="Number" value="25" min="10" max="100" />
	</Story>
</Canvas>

### Disabled

<Canvas>
	<Story name="disabled">
		<Form.Number label="Number" value="25" min="10" max="100" disabled />
	</Story>
</Canvas>

### Read only

<Canvas>
	<Story name="read-only">
		<Form.Number label="Number" value="25" min="10" max="100" readOnly />
	</Story>
</Canvas>

## Interactions

## Usage

N/C

## Accessibility

N/C
