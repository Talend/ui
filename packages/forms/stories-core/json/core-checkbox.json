{
  "jsonSchema": {
    "type": "object",
    "title": "Booleans",
    "properties": {
      "standard": {
        "type": "boolean"
      },
      "autofocused": {
        "type": "boolean"
      },
      "disabled": {
        "type": "boolean"
      }
    },
    "required": ["autofocused"]
  },
  "uiSchema": [
    {
      "key": "standard",
      "title": "Check if you are happy (standard)"
    },
    {
      "key": "autofocused",
      "title": "Check if you are sad (autofocused)",
      "autoFocus": true
    },
    {
      "key": "disabled",
      "title": "Check if you are handicapped (you can't, disabled)",
      "disabled": true
    }
  ],
  "properties": {
    "standard": true
  }
}
