import React from 'react';
import { shallow } from 'enzyme';

import Text from './Text';

describe('Select field', () => {
	const schema = {};

	it('should render simple select', () => {
		// given

		// when

		// then
	});

	it('should render select multiple', () => {
		// given

		// when

		// then
	});

	it('should render select with titleMap', () => {
		// given

		// when

		// then
	});

	it('should render disabled input', () => {
		// given

		// when

		// then
	});

	it('should render readOnly input', () => {
		// given

		// when

		// then
	});

	it('should trigger onChange', () => {
		// given

		// when

		// then
	});

	it('should trigger onChange with number value', () => {
		// given

		// when

		// then
	});

	it('should trigger array onChange', () => {
		// given

		// when

		// then
	});

	it('should trigger array onChange with number value', () => {
		// given

		// when

		// then
	});
});
