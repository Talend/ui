import { Canvas, Meta } from '@storybook/addon-docs';

import tokens from './';

import './index.scss';

<Meta title="Welcome" />

# Talend Design tokens

<Canvas
    withSource="none"
	additionalActions={[
		{
			title: 'Switch mode',
			onClick: () => {
                const playground = document.getElementById('playground');
				const currentTheme = playground.dataset.theme;
				if (currentTheme === 'dark') {
                    playground.dataset.theme = 'light';
				} else {
                    playground.dataset.theme = 'dark';
				}
			},
		},
	]}
>
	<div
		id="playground"
		style={{
			width: '100%',
			color: tokens.coralColorNeutralText,
			background: tokens.coralColorNeutralBackground,
		}}
	>
		<div
			style={{
				display: 'flex',
				alignItems: 'center',
				gap: 10,
				height: 48,
				paddingInline: 10,
				background: tokens.coralColorBrandingNavigation,
			}}
		>
			<div
				style={{
					height: 15,
					width: 15,
					borderRadius: tokens.coralRadiusRound,
					background: tokens.coralBrandingLogo,
					backgroundSize: 'contain',
				}}
			></div>
			<div
				style={{
					height: 15,
					width: 100,
					borderRadius: tokens.coralRadiusS,
					background: tokens.coralColorNeutralBackground,
					opacity: tokens.coralOpacityS,
				}}
			></div>
			<div
				style={{
					height: 15,
					width: 100,
					marginLeft: 'auto',
					borderRadius: tokens.coralRadiusS,
					background: tokens.coralColorNeutralBackground,
					opacity: tokens.coralOpacityS,
				}}
			></div>
		</div>
		<div
			style={{
				display: 'flex',
				flexDirection: 'column',
				alignItems: 'center',
				gap: 10,
				paddingBlock: 20,
				height: 300,
				width: 150,
				background: tokens.coralColorBrandingNavigation,
			}}
		>
			<div
				style={{
					height: 15,
					width: '80%',
					borderRadius: tokens.coralRadiusS,
					background: tokens.coralColorNeutralBackground,
					opacity: tokens.coralOpacityS,
				}}
			></div>
			<div
				style={{
					height: 15,
					width: '80%',
					borderRadius: tokens.coralRadiusS,
					background: tokens.coralColorNeutralBackground,
					opacity: tokens.coralOpacityS,
				}}
			></div>
			<div
				style={{
					height: 15,
					width: '80%',
					borderRadius: tokens.coralRadiusS,
					background: tokens.coralColorNeutralBackground,
					opacity: tokens.coralOpacityS,
				}}
			></div>
		</div>
	</div>
</Canvas>
