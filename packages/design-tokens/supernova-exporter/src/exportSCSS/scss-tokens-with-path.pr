{[ const designSystem = ds.currentDesignSystem() /]}
{[ const filename = "_index.scss" /]}
{[ const folderName = getThemeName(designSystem.name) /]}

{[ if (folderName === "light") ]}
{{ filename }}
{[ else ]}
{{ folderName }}/{{ "_tokens.scss" }}
{[/]}

