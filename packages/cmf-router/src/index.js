import { routerActions } from 'react-router-redux/lib/actions';
import defaultRouterConfiguration from './defaultRouterConfiguration';
import * as route from './route';

export { route, routerActions };
export default defaultRouterConfiguration;
