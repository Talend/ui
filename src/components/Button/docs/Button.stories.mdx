import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { ThemeProvider } from 'styled-components';
import { action } from '@storybook/addon-actions';
import { Col, Row } from '../../../docs';

import Button from '../';
import btnZoning from './images/buttons-zoning.jpg';
import btnBehaviors from './images/buttons-behaviors.jpg';
import btnOrder from './images/buttons-order.jpg';
import theme from '../../../themes';

<Meta title="Components/Button" component={Button} />

# Button

Buttons are clickable items used for actions. They can have different style depending on the needs.

## Style

## Zoning

<img src={btnZoning} alt="zoning image for buttons" className="full" class="full" />

## Variations

<Preview>
  <Story name="primary">
		<Button.Primary onClick={action('clicked')}>Basic Button</Button.Primary>
		<Button.Primary small onClick={action('clicked')}>
			Small Button
		</Button.Primary>
		<Button.Primary disabled onClick={action('clicked')}>
			Disabled Button
		</Button.Primary>
		<Button.Primary disabled focusable onClick={action('clicked')}>
			Disabled Focusable Button
		</Button.Primary>
	</Story>
    <Story name="destructive">
		<Button.Destructive onClick={action('clicked')}>Basic Button</Button.Destructive>
        <Button.Destructive small onClick={action('clicked')}>
            Small Button
        </Button.Destructive>
        <Button.Destructive disabled onClick={action('clicked')}>
            Disabled Button
        </Button.Destructive>
        <Button.Destructive disabled focusable onClick={action('clicked')}>
            Disabled Focusable Button
        </Button.Destructive>
	</Story>
	  <Story name="secondary">
		<Button.Secondary onClick={action('clicked')}>Basic Button</Button.Secondary>
		<Button.Secondary small onClick={action('clicked')}>
			Small Button
		</Button.Secondary>
		<Button.Secondary disabled onClick={action('clicked')}>
			Disabled Button
		</Button.Secondary>
		<Button.Secondary disabled focusable onClick={action('clicked')}>
			Disabled Focusable Button
		</Button.Secondary>
	</Story>
	  <Story name="ghost">
		<Button.Ghost onClick={action('clicked')}>Basic Button</Button.Ghost>
		<Button.Ghost small onClick={action('clicked')}>
			Small Button
		</Button.Ghost>
		<Button.Ghost disabled onClick={action('clicked')}>
			Disabled Button
		</Button.Ghost>
		<Button.Ghost disabled focusable onClick={action('clicked')}>
			Disabled Focusable Button
		</Button.Ghost>
	</Story>
	  <Story name="icon">
		<Button.Icon onClick={action('clicked')} icon={'plus'}>
			Basic Button
		</Button.Icon>
		<Button.Icon small onClick={action('clicked')} icon={'plus'}>
			Small Button
		</Button.Icon>
		<Button.Icon disabled onClick={action('clicked')} icon={'plus'}>
			Disabled Button
		</Button.Icon>
		<Button.Icon disabled focusable onClick={action('clicked')} icon={'plus'}>
			Disabled Focusable Button
		</Button.Icon>
	</Story>
</Preview>

## Sizes

Cobalt support two buttons sizes: **default** and **small**.

### Default

| Attribute                      | Variable      |
| ------------------------------ | ------------- |
| height                         | 3.4rem        |
| padding-left; padding-right    | $large       |
| border                         | solid, 0.1rem |
| border-radius                  | rectRadius    |
| _ (if icon) _ icon-size        | 1.4rem        |
| _ (if icon) _ margin-right     | $smaller     |
| _ (for dropdown) _ icon-size   | 0.8rem        |
| _ (for dropdown) _ margin-left | $smaller     |

### Small

The small size is available for primary, secondary and ghost buttons — but shouldn't be use for buttons with icons or dropdown buttons.

| Attribute                   | Variable      |
| --------------------------- | ------------- |
| height                      | 2.8rem        |
| padding-left; padding-right | $small       |
| border                      | solid, 0.1rem |
| border-radius               | rectRadius    |

### Icon buttons

Icon buttons has two sizes: **large** and **small**. They can also be used as dropdown but without the caret icon.

| Large                | Variable      | Small         | Variable      |
| -------------------- | ------------- | ------------- | ------------- |
| icon-size            | 2.4 rem       | icon-size     | 1.6rem        |
| **If circle border** |
| size                 | 2.4 rem       | size          | 1.6rem        |
| border               | solid, 0.2rem | border        | solid, 0.2rem |
| icon-size            | solid, 1.2rem | icon-size     | solid, 0.8rem |
| border-radius        | rectCircle    | border-radius | rectCircle    |

## Colors

## Fonts

| Property          | font-size | font-weight | colors      |
| ----------------- | --------- | ----------- | ----------- |
| Primary           | 1.4rem    | semi-bold   | $white     |
| Secondary & Ghost | 1.4rem    | semi-bold   | $st-tropaz |
| Small             | 1.4rem    | semi-bold   | $variable  |

## States

<img src={btnBehaviors} alt="behaviors image for buttons" className="full" class="full" />

|              | Primary                                              | Destructive                                          | Secondary                                        | Ghost                                     | Icon                          |
| ------------ | ---------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------ | ----------------------------------------- | ----------------------------- |
| **Hover**    | background-color: shade ($st-tropaz, 20%)           | background-color: shade ($chestunut-red, 20%)       | background-color: tint ($st-tropaz, 10%)        | background-color: tint ($st-tropaz, 10%) | color: tint ($variable, 20%) |
| **Pressed**  | background-color: shade ($st-tropaz, 40%)           | background-color: shade ($chestunut-red, 40%)       | background-color: tint ($st-tropaz, 30%         | background-color: tint ($st-tropaz, 30%) | color: tint ($variable, 40%) |
| **Disabled** | background-color: $alto; color: $black opacity 0.54; | background-color: $alto; color: $black opacity 0.54; | border-color: $alto; color: $black opacity 0.54; | color: $black opacity 0.54;              | color: $black opacity 0.54;  |

## Content

- Except in some very specific situations, the button must contain a maximum of 3 words action

- Use sentence-style capitalization (only the first word in a phrase and any proper nouns capitalized)

## Interaction

- **Buttons should all have a tooltip on hover displaying the description**

<Preview>
  <Story name="basic">
		<Button onClick={action('clicked')}>Basic Button</Button>
		<Button small onClick={action('clicked')}>
			Small Button
		</Button>
		<Button disabled onClick={action('clicked')}>
			Disabled Button
		</Button>
		<Button disabled focusable onClick={action('clicked')}>
			Disabled Focusable Button
		</Button>
	</Story>
</Preview>

- **Buttons has a loading states**

When the user clicks on the button, a spinner will appear on the left of the label. No actions will be possible until it's loading.
In case of button with icon inside, the spinner will replace the icon as long as it's loading.

// TODO add demo

## Skeleton

| Attribute        | Variable             |
| ---------------- | -------------------- |
| height           | 3.4rem               |
| width            | 8rem                 |
| border-radius    | rectRadius           |
| background-color | shade ($black, 25%) |

// TODO add demo

## Accessibility

Classic behaviors expected:

- Press <kbd>TABS</kbd> to focus on the button

- Press <kbd>SPACE</kbd> or <kbd>ENTER</kbd> to activate the button

For more information, please go to accessibility page

## Usage

<Preview>
  <Story
    name="usage"
    height="20rem"
    args={{
      children: 'Lorem ipsum',
    }}
  >
    { props => <Button {...props} onClick={action('clicked')} />}
  </Story>
</Preview>

<Props story="usage" />

### Buttons order

<img src={btnOrder} alt="button order in a page" className="full" class="full" />

When using multiple buttons, **the primary button appears to the right** and any secondary buttons appear to the left.

## Changelog

| Date       | Author | Description |
| ---------- | ------ | ----------- |
| 12/06/2019 | Monia  | Init page   |
