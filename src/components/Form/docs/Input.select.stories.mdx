import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { FigmaImage, Use } from '../../../docs';

import Form from '../';
import { Col, Row } from '../../../docs';

<Meta title="Components/Form/Fields/Select" />

# Select

Select lets the user choose one option or multiple options from an options menu. 

## Zoning

<FigmaImage
	src="https://www.figma.com/file/qc8oUSyVJM67gSsbTShftt/%F0%9F%93%90-Zoning?node-id=51%3A122"
	alt="zoning image for input select"
/>
## Style

## States

### Default

<Canvas>
    <Story name="default">
        <Form.Select label="Select" values={['', 'foo', 'bar']}/>
    </Story>
    <Story name="disabled">
        <Form.Select label="Disabled Select" disabled  values={['', 'foo', 'bar']}/>
    </Story>
    <Story name="read-only">
        <Form.Select label="Read-Only Select" readOnly value="foo" values={['', 'foo', 'bar']} />
    </Story>
</Canvas>

### Multiple

<Canvas>
    <Story name="default multiple">
        <Form.Select label="Select" values={['', 'foo', 'bar']} multiple />
    </Story>
    <Story name="disabled multiple">
        <Form.Select label="Disabled Select" disabled  values={['', 'foo', 'bar']} multiple/>
    </Story>
    <Story name="read-only multiple">
        <Form.Select label="Read-Only Select" readOnly value={['','bar']} values={['', 'foo', 'bar']} multiple/>
    </Story>
</Canvas>

### With option group

<Canvas>
    <Story name="default object">
        <Form.Select label="Select" values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}} />
    </Story>
    <Story name="disabled object">
        <Form.Select label="Disabled Select" disabled  values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}}/>
    </Story>
    <Story name="read-only object">
        <Form.Select label="Read-Only Select" readOnly value="foo" values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}}/>
    </Story>
</Canvas>

### Multiple with option group

<Canvas>
    <Story name="default multiple object">
        <Form.Select label="Select" values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}} multiple />
    </Story>
    <Story name="disabled multiple object">
        <Form.Select label="Disabled Select" disabled  values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}} multiple />
    </Story>
    <Story name="read-only multiple object">
        <Form.Select label="Read-Only Select" readOnly value={['','bar']} values={{foo: ['', 'foo', 'bar'], bar: ['', 'foo', 'bar']}} multiple />
    </Story>
</Canvas>

## Content


## Interaction

- When a user clicks into the text input field, they trigger the selection menu which is the browser default one (for compatibility reason).

## Code

To do

## Accessibility

The keyboard gestures are the default ones, such as:
- Press <kbd>DOWN ARROW KEY</kbd> to display and navigate into the menu.
- Press <kbd>ENTER</kbd> or <kbd>SPACE</kbd> to select the option.

## Changelog

| Date       | Author | Description |
| ---------- | ------ | ----------- |
| 12/06/2020 | Monia Huet  | Init page   |

