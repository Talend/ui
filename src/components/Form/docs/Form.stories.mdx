import { Meta, Preview, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import Form from '../';
import Button from '../../Button';

import CountryCodes from './data/CountryCodes.json';

export function getCountryCodes() {
    return CountryCodes.map(({ name, dial_code }) => `${name} (${dial_code})`);
}
export function getColumnTypes() {
    return ['String', 'Long', 'Date', 'Boolean', 'Decimal'];
}

<Meta title="Components/Form"/>

# Form

<Preview>
    <Story name="Overview">
        <div style={{ display: 'flex', justifyContent: 'center', margin: '0 auto', width: '60vw', maxWidth: '70rem'}}>
            <Form>
                <Form.Fieldset legend="Form fields">
                    <Form.Text label="Input" />
                    <Form.InputGroup label="Phone" prefix={<Form.Select label="Phone prefix" values={getCountryCodes()} value="France (+33)" style={{ width: '14rem' }}/>}>
                        <Form.Tel label="Phone number" />
                    </Form.InputGroup>
                    <Form.InputGroup label="Column" suffix={<Form.Select label="Column type" values={getColumnTypes()} value="Date" />}>
                        <Form.Text label="Column name" />
                    </Form.InputGroup>
                    <Form.InputGroup label="Amount"
                                     prefix="$"
                                     suffix=".00">
                        <Form.Number label="Amount without decimal" min="1" step="1" />
                    </Form.InputGroup>
                    <Form.File label="File" />
                    <Form.Password label="Password" />
                    <Form.Datalist label="Datalist" values={['foo', 'bar']} />
                    <Form.Search label="Search" />
                    <Form.Textarea label="Textarea" />
                    <Form.Select label="Select" values={['', 'foo', 'bar']} value="foo"/>
                    <Form.Select label="Select multiple" values={['foo', 'bar']} multiple value={['foo']} />
                    <Form.Checkbox label="Checkbox" checked />
                    <Form.Radio label="Radio" checked />
                    <Form.Switch label="Switch" checked />
                </Form.Fieldset>
                <div style={{ display: 'flex', justifyContent: 'space-between', padding: '2rem 0'}}>
                    <Button.Secondary type="reset" onClick={action('clicked')}>Reset</Button.Secondary>
                    <Button.Primary onClick={action('clicked')}>Submit</Button.Primary>
                </div>
            </Form>
        </div>
    </Story>
</Preview>