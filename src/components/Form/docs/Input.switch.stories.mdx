import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { FigmaImage, Use } from '../../../docs';
import Form from '../';
import { Col, Row } from '../../../docs';

<Meta title="Components/Form/Fields/Switch"/>

# Switch

Switches are a quick way to view and switch between two states or on-off state instantly. 

## Zoning

<FigmaImage src="https://www.figma.com/file/2Sw2IvUHZ9F2srJ3L8AWxr/%F0%9F%93%90-Zoning?node-id=36%3A0" alt="zoning image for switch" />

## Style

### Variations

**With label**

// add demo

**Without label**

// add demo

## States

### Default

<Canvas>
    <Story name="default">
        <Form.Switch label="Switch off" />
        <Form.Switch label="Switch on" checked />
    </Story>
</Canvas>

### Disabled

<Canvas>
    <Story name="disabled">
        <Form.Switch label="Disabled Switch off" disabled />
        <Form.Switch label="Disabled Switch on" checked disabled />
    </Story>
</Canvas>

### Read-only

<Canvas>
    <Story name="read-only">
        <Form.Switch label="Read-only Switch off" readOnly />
        <Form.Switch label="Read-only Switch on" checked readOnly />
    </Story>
</Canvas>

### Default with 2 values

<Canvas>
    <Story name="default with 2 values">
        <Form.Switch label="Switch on" values={['off', 'on']} value="on" />
        <Form.Switch label="Switch off" values={['basic', 'advanced']} />
    </Story>
</Canvas>

### Disabled with 2 values

<Canvas>
    <Story name="disabled with 2 values">
        <Form.Switch label="Switch on" values={['off', 'on']} value="on" disabled />
        <Form.Switch label="Switch off" values={['this is off', 'and of course this is on']} disabled />
    </Story>
</Canvas>

### Read-only with 2 values

<Canvas>
    <Story name="read-only with 2 values">
        <Form.Switch label="Switch on" values={['off', 'on']} value="on" readOnly />
        <Form.Switch label="Switch off" values={['this is off', 'and of course this is on']} readOnly />
    </Story>
</Canvas>

### Default with 3 values

<Canvas>
    <Story name="default with 3 values">
        <Form.Switch label="Switch one/two/three" values={['one', 'two', 'three']}  value="two"/>
        <Form.Switch label="Switch input/both/output" values={['input', 'both', 'output']} />
    </Story>
</Canvas>

### Disabled with 3 values

<Canvas>
    <Story name="disabled with 3 values">
        <Form.Switch label="Switch one/two/three" values={['one', 'two', 'three']}  value="two" disabled />
        <Form.Switch label="Switch input/both/output" values={['input', 'both', 'output']} disabled />
    </Story>
</Canvas>

### Read-only with 3 values

<Canvas>
    <Story name="read-only with 3 values">
        <Form.Switch label="Switch one/two/three" values={['one', 'two', 'three']}  value="two" readOnly />
        <Form.Switch label="Switch input/both/output" values={['input', 'both', 'output']} readOnly />
    </Story>
</Canvas>

When the switch is disabled the whole component is displayed with 0.54 opacity


## Content

To do

- Not use commas or semicolons at the end of each line
- Each label should start with a capital letter

## Interaction

- Use a tooltip on hover to explain the user what happens when the switch is on.

// ADD DEMO

## Code

To do

## Accessibility

Press <kbd>TABS</kbd> to focus on a switch.

Press the <kbd>ENTER</kbd> or <kbd>SPACE</kbd> to toggle the switch between selected and not selected. 
 

## Changelog

| Date       | Author | Description |
| ---------- | ------ | ----------- |
| 06/16/2020 | Monia Huet  | Init page   |


