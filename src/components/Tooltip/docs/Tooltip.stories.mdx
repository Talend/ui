import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import Tooltip from '../.';
import Button from '../../Button';
import { Col, Row } from '../../../docs';

<Meta title="Components/Tooltip" component={Tooltip}/>

# Tooltip

Tooltips provide helpful, additional content for their paired elements on hover. They are contextual and specific to the element and don't provide the big picture. 

<Preview>
  <Story name="top">
		<Tooltip title="Relevant information about this basic button" placement="top" visible>
			<Button onClick={action('clicked')}>Top</Button>
		</Tooltip>
  </Story>
  <Story name="left">
		<Tooltip title="Relevant information about this basic button" placement="left">
			<Button onClick={action('clicked')}>Left</Button>
		</Tooltip>
  </Story>
  <Story name="bottom">
		<Tooltip title="Relevant information about this basic button" placement="bottom">
			<Button onClick={action('clicked')}>Bottom</Button>
		</Tooltip>
  </Story>
  <Story name="right">
		<Tooltip title="Relevant information about this basic button" placement="right">
			<Button onClick={action('clicked')}>Right</Button>
		</Tooltip>
  </Story>
</Preview>

## Style

## Sizes

### Default

| Attribute                   | Variable     |
| --------------------------- | ------------ |
| height                      | 2rem         |
| max-height                  | 5rem         |
| max-width                   | 25rem        |
| padding-left; padding-right | $smaller    |
| border-radius               | rectRadius   |
| **Pointer details**         |
| width                       | 0.6rem       |
| height                      | 0.4rem       |
| position                    | left-aligned |

## Colors

| Attribute        | Variable    |
| ---------------- | ----------- |
| background-color | $dove-gray |
| color            | $white     |

## Fonts

| Attribute | Variable |
| --------- | -------- |
| font-size | 1.2rem   |

## Content
Tooltips with obvious or redundant text are not beneficial to users. If you can’t think of particularly helpful content, don’t offer a tooltip. Otherwise, you’ll just add information pollution to your UI and waste the time of users.

### Do
- Use tooltips consistently throughout your site.
- Try to make the text as short as possible and include the most critical or meaningful info to make reading easier. Add a popup to display detailed info.
- If the tooltip has more than one sentence, use punctuations/periods.

### Don't
- Don't use tooltips for simple actions users perform regularly.
- Avoid tooltips which are complete duplicates of what is stated clearly in the UI.
- Don't use tooltips for information that is valid to task completion, for example field or password requirements. Such info essential to a frictionless user experience should always be present on the screen
- Don't use period at the end of a tooltip even if it's a complete sentence in order not to overload the interface.

<hr />

## Usage

<Preview>
  <Story
    name="usage"
    args={{
        text: 'Example'
    }}
  >
    {({ text, ...props }) => <Tooltip {...props}><Button onClick={action('clicked')}>{text}</Button></Tooltip>}
  </Story>
</Preview>

<Props story="usage" />

## Accessibility

Classic behaviors expected:

- Press <kbd>TABS</kbd> to focus element and tooltip will be visible

For more information, please go to accessibility page

## Changelog

| Date       | Author | Description    |
| ---------- | ------ | -------------- |
| 12/19/2019 | Monia  | Init page      |
| 12/31/2019 | Fabien | Add basic demo |
