import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { FigmaImage } from '../../../docs';
import Button from '../../Button';
import Link from '../../Link';
import Dropdown from '../';

<Meta title="Components/Dropdown" component={Dropdown} />

# Dropdown

A dropdown menu displays a list of actions to take.

## Style

## Zoning

<FigmaImage src="https://www.figma.com/file/2Sw2IvUHZ9F2srJ3L8AWxr/%F0%9F%93%90-Zoning?node-id=1%3A22" alt="zoning image for dropdown" />

## States

// ADD DEMO

## Content

**Keep menu items to a single line of text as much as possible**

If not, when the label is too long for the available horizontal space, they wrap to form another line.

## Interaction

<Preview>
  <Story name="information">
	<Dropdown
		as={Button}
		aria-label="Custom menu"
		items={[
			<button>Custom item 1</button>,
			<button>Custom item 2</button>,
			<button>Custom item 3</button>,
		]}
	>
		Menu
	</Dropdown>
  </Story>
</Preview>

- **Dropdown menu items should all have a tooltip on hover displaying the description**

// To Do Add story

## Skeleton


// TODO add demo

## Accessibility

- Press <kbd>Tabs</kbd> to focus on the dropdown

- Press <kbd>Up Arrow</kbd> or <kbd>Down Arrow</kbd> to navigate in the dropdown menu

- Press <kbd>Space</kbd> or <kbd>Enter</kbd> to activate the dropdown menu item

- Press <kbd>Escape</kbd> to close the dropdown menu

## Usage

**Max height**

The max-height of a dropdown is 32rem. A scroll-bar has to be added after exceeding 32rem longer items.
This rule is not applicable for the header-bar dropdowns.

<Preview>
  <Story
    name="usage"
    height="20rem"
    args={{
      children: 'Lorem ipsum',
    }}
  >
    {({ children, ...rest}) => <Dropdown
                                {...rest}
                                as={Link}
                           		items={[
                                   <button>Custom item 1</button>,
                                   <button>Custom item 2</button>,
                                   <button>Custom item 3</button>,
                               ]}
                            >
                                {children}
                            </Dropdown>}
  </Story>
</Preview>

<Props story="usage" />

## Changelog

| Date       | Author | Description |
| ---------- | ------ | ----------- |
| 01/07/2020 | Monia  | Init page   |
