import { Canvas, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { VisuallyHidden } from 'reakit/VisuallyHidden';

import { FigmaImage } from '../../../docs';

import { PortalOnBoarding as pobStory, Portal as pStory, Apps as aStory } from './HeaderBar';

import dark from '../../../themes/dark.theme';

import HeaderBar from '../';

<Meta title="Components/HeaderBar" />

# Header Bar

The header bar is the place, where the user finds useful information and tools to for the application.
The header is placed on the top and takes the full window width.

## Zoning

<FigmaImage src="https://www.figma.com/file/2Sw2IvUHZ9F2srJ3L8AWxr/%F0%9F%93%90-Zoning?node-id=10%3A88" alt="zoning image for buttons" />

## Style

### Variations

#### Portal Onboarding

Only Talend Logo should be visible on the left and no other elements are displayed on the right.

<Preview>
	<Story name="portal-onboarding">{pobStory.bind({})}</Story>
</Preview>

#### Portal

The Talend logo should be linked to the Portal homepage.

<Preview>
	<Story name="portal">{pStory.bind({})}</Story>
</Preview>

#### In the apps

The Talend logo should be linked to the Portal.

<Preview>
	<Story name="apps">{aStory.bind({})}</Story>
</Preview>

### App switcher

The App switcher displays ALL applications that the user has access to. No entry in the app list should be disabled. Entries that cannot be used (e.g. no permissions) shouldn't be displayed at all (no disabled state). 
There is no selected state, from the same app user can select the app again and the order of the application names remain always the same.

### In-product chat

By clicking on the chat button at any time, a chat widget is displayed allowing users to send messages to the Talend team to provide feedback or questions.

### User  Menu

All versions of the product header bar give access to the user menu.  The content vary depending on the application and the application context (on premise)

### Product specific actions

Optionally, products may add specific actions as buttons, dropdown or links. 


## Accessibility

Press <kbd>TABS</kbd> to focus on an item

Press <kbd>ENTER</kbd> or <kbd>SPACE</kbd> to select an item, expand and collapse item with children


## Changelog

| Date       | Author     | Description |
| ---------- | ---------- | ----------- |
| 08/26/2020 | Monia Huet | Init page   |
