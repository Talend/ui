const arrowLeft = require('./arrow-left.svg') as string;
const arrowRight = require('./arrow-right.svg') as string;
const caret = require('./caret.svg') as string;
const check = require('./check.svg') as string;
const cross = require('./cross.svg') as string;
const datagrid = require('./datagrid.svg') as string;
const eye = require('./eye.svg') as string;
const eyeSlash = require('./eye-slash.svg') as string;
const information = require('./information.svg') as string;
const link = require('./link.svg') as string;
const minus = require('./minus.svg') as string;
const opener = require('./opener.svg') as string;
const plus = require('./plus.svg') as string;
const search = require('./search.svg') as string;
const talend = require('./talend.svg') as string;
const talendLogo = require('./talend-logo.svg') as string;
const upload = require('./upload.svg') as string;
const validate = require('./validate.svg') as string;
const warning = require('./warning.svg') as string;

const icons: { [key: string]: string } = {
	arrowLeft,
	arrowRight,
	caret,
	cross,
	check,
	datagrid,
	eye,
	eyeSlash,
	information,
	link,
	minus,
	opener,
	plus,
	search,
	talend,
	talendLogo,
	upload,
	validate,
	warning,
};

export default icons;
