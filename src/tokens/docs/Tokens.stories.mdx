import { Meta, Story } from '@storybook/addon-docs/blocks';
import { ColorPalette, ColorItem, Typeset } from '@storybook/components';
import { Table, ColorSwatches } from '../../docs';
import tokens from './../index';
import colors, { brand, grayscale, palette, swatches } from './../colors.tokens';

<Meta title="Design Tokens/Design Tokens" />

# Design Tokens

We are using <a href="https://system-ui.com/theme/" target="_blank">the System UI theme specification</a>

## Space

<Table data={tokens.space} />

## Colors

### Grayscale

<ColorSwatches title="tokens.colors.gray" subtitle="All grays" color="gray" colors={colors} />

### Brand

<ColorPalette>
	<ColorItem title="tokens.colors.brand" subtitle="Brand colors" colors={brand} />
</ColorPalette>

<ColorPalette>
	<ColorItem
		title="tokens.colors.gradients"
		colors={{ twilight: colors.twilight.backgroundImage }}
	/>
</ColorPalette>

### Interactive colors

<ColorPalette>
	<ColorItem title="Clickable color" colors={{ lochmara: colors.lochmara }} />
</ColorPalette>

<ColorPalette>
	<ColorItem title="Focus color" colors={{ moodyPurple: colors.moodyPurple }} />
</ColorPalette>

### Color swatches

<div>
	{Object.keys(palette).map(color => (
		<ColorSwatches key={color} title={`tokens.colors.${color}`} color={color} colors={swatches} />
	))}
</div>

## fontSizes

<Typeset fontSizes={Object.values(tokens.fontSizes)} />

## fonts

Default content uses sans-serif font.

<Typeset fontSizes={[12, 14, 16, 20, 24, 32, 40, 48]} fontFamily={tokens.fonts.sansSerif} />

Data related content uses monospace font.

<Typeset fontSizes={[12, 14, 16, 20, 24, 32, 40, 48]} fontFamily={tokens.fonts.monospace} />

## fontWeights

<Table data={tokens.fontWeights} />

## lineHeights

<Table data={tokens.lineHeights} />

## letterSpacings

<Table data={tokens.letterSpacings} />

## sizes

<Table data={tokens.sizes} />

## borders

<Table data={tokens.borders} />

## borderWidths

<Table data={tokens.borderWidths} />

## borderStyles

<Table data={tokens.borderStyles} />

## opacity

<Table data={tokens.opacity} />

## radii

<Table data={tokens.radii} />

## shadows

<Table data={tokens.shadows} />

## zIndices

<Table data={tokens.zIndices} />

## transitions

<Table data={tokens.transitions} />

## breakpoints

<Table data={tokens.breakpoints} />
