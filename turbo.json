{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build:lib": {
      "dependsOn": ["^build:lib"],
      "outputs": ["lib/**", "dist/**"]
    },
    "build:lib:esm": {
      "dependsOn": ["^build:lib:esm"],
      "outputs": ["lib-esm/**"]
    },
    "pre-release": {
      "dependsOn": ["^pre-release"],
      "outputs": ["dist/**"]
    },
    "start": {},
    "storybook:build": {
      "dependsOn": ["^storybook:build"],
      "outputs": ["storybook-static/**"]
    },
    "lint": {},
    "check-types": {},
    "test:cov": {
      "dependsOn": ["^test:cov"],
      "outputs": ["coverage/**", "**/*.snap"]
    },
    "test:demo": {
      "dependsOn": ["^test:demo"],
      "outputs": ["storybook-static/**", "jsdoc/**"]
    },
    "format:write": {},
    "format:check": {}
  }
}
